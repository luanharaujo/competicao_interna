/*teste do braço*/
/*Definicições*/
//moteore
#define ME OUT_A
#define MD OUT_C
#define MDE OUT_AC
#define MB OUT_B
//sensores
#define US IN_4
#define LSD IN_2
#define LSE IN_3
#define RGB IN_1
//parametros
#define POTMAX 75

task main()
{
 int tc, old_tc;
 OnFwd(MB,-70);
 tc =  MotorTachoCount(MB);
 while(tc == MotorTachoCount(MB)){}
 do{
    old_tc = tc;
    tc =  MotorTachoCount(MB);
    Wait(15);
 } while(old_tc != tc)
 OnFwd(MB,0);
 TextOut(0,LCD_LINE2,"Hull!!");
 Wait(1000);
 TextOut(0,LCD_LINE3,"Hull!!2");


 ResetTachoCount(MB);
 ResetTachoCount(MB);
 ResetTachoCount(MB);
 //Wait(200);
 NumOut(0,LCD_LINE8,MotorTachoCount(MB));


 OnFwd(MB,70);
 NumOut(0,LCD_LINE4,MotorTachoCount(MB));
 while(MotorTachoCount(MB)<28){
    NumOut(0,LCD_LINE1,MotorTachoCount(MB));
 }
 NumOut(0,LCD_LINE5,MotorTachoCount(MB));
 OnFwd(MB,0);
 
 while(1){}
}
